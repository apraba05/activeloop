# Local Redis Stack with RediSearch (vector KNN).
# run.sh uses `docker run` equivalently when the compose plugin is absent.
services:
  redis:
    image: redis/redis-stack-server:7.2.0-v10
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
